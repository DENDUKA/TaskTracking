namespace TaskTrackingSystem.Logic.Email
{
    //public static class EmailHelper
    //{
    //    public static string RenderRazorViewToString(string viewName, object model)
    //    {
    //        var TempData = new TempDataDictionary();
    //        var viewData = new ViewDataDictionary
    //        {
    //            Model = model
    //        };

    //        using (var sw = new StringWriter())
    //        {
    //            var viewResult = ViewEngines.Engines.FindPartialView(new ControllerContext(), viewName);
    //            var viewContext = new ViewContext(new ControllerContext(), viewResult.View, viewData, TempData, sw);
    //            viewResult.View.Render(viewContext, sw);
    //            viewResult.ViewEngine.ReleaseView(new ControllerContext(), viewResult.View);
    //            return sw.GetStringBuilder().ToString();
    //        }
    //    }
    //}
}